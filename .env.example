# Exemplo de arquivo .env para o Bot Wiki
# Renomeie este arquivo para .env e ajuste os valores conforme necessário

# Nome da aplicação
APP_NAME=bot-wiki

# Versão da aplicação
APP_VERSION=1.0.0

# Porta em que a API irá rodar
APP_PORT=3000

# Ambiente de execução (development, production)
NODE_ENV=development

# Debug do Node.js (use "*:dev" para ver logs de desenvolvimento)
DEBUG="*:dev"

# Configurações do banco de dados PostgreSQL
# Host do banco (use 'db' se estiver usando Docker Compose)
DB_CONNECT_HOST=db
# Porta do banco
DB_CONNECT_PORT=5432
# Usuário do banco
DB_CONNECT_USERNAME=postgres
# Senha do banco
DB_CONNECT_PASSWORD=1234
# Nome do banco de dados
DB_CONNECT_DATABASE=wiki
# Dialeto do banco (postgres, mysql, etc)
DB_CONNECT_DIALECT=postgres

# Salvar arquivos (true/false)
SAVE_FILES=true
